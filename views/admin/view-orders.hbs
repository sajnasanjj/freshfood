
<div class="container">

<table class="table">
   
  <thead>

    <tr>
      <th scope="col">SI</th>
      <th scope="col">Date</th>
      <th scope="col">Location</th>
      <th scope="col">Mobile</th>
      <th scope="col">Total</th>
      <th scope="col">Payment</th>
     <th scope="col">Status</th>
     {{!-- <th scope="col">Details</th> --}}

    </tr>
  </thead>
  <tbody>
       {{#each order}}
    <tr>
      <td>{{inc @index}}</td>
      <td>{{this.date}}</td>
      <td>{{this.deliveryDetails.location}}</td>
      <td>{{this.deliveryDetails.Mobile}}</td>
      <td>{{this.total}}</td>
      <td>{{this.paymentMethod}}</td>
      <td>{{this.status}}</td>
    <td> 
      {{#if cancel}}
      <button class="btn btn-danger">Canceled</button>
      {{else if deliver}}
      <button class="btn btn-success">Delivered</button>
      {{else}}
      <div class="dropdown">
      <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-expanded="false">
      {{this.status}}
  </a>
  <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
    <a class="dropdown-item" href="/admin/status-change?id={{this._id}}&name=canceled">canceled</a>
    <a class="dropdown-item" href="/admin/status-change?id={{this._id}}&name=delivered">delivered</a>
    <a class="dropdown-item" href="/admin/status-change?id={{this._id}}&name=placed">placed</a>

  </div>
  </div>
  
 {{/if}}


  </td>
  
			{{!-- <td><a href="/view-order-proo/{{this._id}}"><button class="btn-sm btn-outline-secondary">View</button></a></td> --}}
      {{!-- <td><button class="btn btn-primary" onclick="sweetalertClick1('/admin/edit-foods/{{this._id}}')">Edit</button>
       <button  class="btn btn-danger " onclick="sweetalertClick2('/admin/delete-foods/{{this._id}}')"> Delete</button></td>
     --}}
    </tr>
   
   
  </tbody>
  {{/each}}
</table>

</div>

<script>

  
  function sweetalertClick1(value){
  Swal.fire({
  title: 'Are you sure?',
  text: "You won't be able to revert this!",
  icon: 'warning',
  showCancelButton: true,
  confirmButtonColor: '#3085d6',
  cancelButtonColor: '#d33',
  confirmButtonText: 'Yes, Edit it!'
}).then((result) => {
  if (result.isConfirmed) {
    window.location =value
   
  }
})
}
  function sweetalertClick2(value1){
  Swal.fire({
  title: 'Are you sure?',
  text: "You won't be able to revert this!",
  icon: 'warning',
  showCancelButton: true,
  confirmButtonColor: '#3085d6',
  cancelButtonColor: '#d33',
  confirmButtonText: 'Yes, delete it!'
}).then((result) => {
  if (result.isConfirmed) {
    {window.location=value1
    }
  }
  })
}
</script>